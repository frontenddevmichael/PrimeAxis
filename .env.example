# EmailJS — copy this file to .env and fill in your credentials
# These are not secrets (EmailJS public key is designed for client-side use),
# but env vars prevent accidental exposure in version control and allow
# per-environment configuration.
VITE_EJS_PUBLIC_KEY=
VITE_EJS_SERVICE_ID=
VITE_EJS_TEMPLATE_ID=
